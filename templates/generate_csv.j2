hostname,device_ip,current_version,target_version,model,target_version_satisfied,target_image_presence,md5_checksum_success,error
{% for host, data in host_results.items() %}
{{ data.hostname }},{{ data.device_ip }},{{ data.current_version }},{{ data.target_version }},{{ data.model }},{{ data.target_version_satisfied }},{{ data.target_image_presence }},{{ data.md5_checksum_success }},{{ data.error }}
{% endfor %}
